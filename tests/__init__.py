"""The tests folder contains test run with pytest.

It mocks the underlying requests, and feeds them up into mocks of the classes.
Most of the actual tests are run on "successful instanstiations" of the classes themselves.
"""
